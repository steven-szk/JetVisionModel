# cv2 / torch / tensorrt from Jetpack (these are built against numpy 1.x)
# onnxruntime needs numpy>=1.21.6 (system has 1.21.5), but cap <2 so JetPack's
# cv2/torch don't break under numpy 2.x's new ABI.
numpy<2
pillow==9.0.1

# onnxruntime-gpu for inference/infermodel.py (Jetson, JetPack 6.2 / CUDA 12.6 / Py3.10).
# Jetson AI Lab index -- use .io (not .dev/.com). Left unpinned on purpose: this index
# prunes old versions, so let pip take whatever's currently there (1.24.0 as of now).
--extra-index-url https://pypi.jetson-ai-lab.io/jp6/cu126
onnxruntime-gpu

smbus2==0.6.1

# evdev (keytrigger.py -- reads the Jetson's own keyboard): install from apt, prebuilt
# for aarch64. pip fails here (no wheel -> source build -> setuptools/packaging clash):
#   sudo apt install python3-evdev

# spidev from apt install
# sudo apt install python3-spidev
# Jetson.GPIO ships with JetPack; if missing: sudo pip3 install Jetson.GPIO