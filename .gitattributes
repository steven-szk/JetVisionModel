# Keep shell scripts and unit files LF so they run on the Jetson even when edited
# on Windows (a CRLF shebang breaks with "bad interpreter: .../bash^M").
*.sh        text eol=lf
*.service   text eol=lf
